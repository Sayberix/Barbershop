git checkout -- LICENSE.php	/* команда отмняет все изменения до последнего коммита в файле LICENSE.php	*/
git log --pretty=format:"%h - %an, %ar : %s"		/* отображение лога в краткой форме	*/
git log --since=2.week					/* показать коммиты за последние 2 недели	*/
git log -p -2						/* 2 последних коммита */

git config --global core.editor "'C:\Program Files\Notepad++\notepad++.exe' -multiInst -notabbar -nosession -noPlugin"
/* использование Noterpad++ редактором комментариев	*/

git checkout -b new_f		/*   создание ветки и одновременный переход в ветку new_f	*/

git remote add origin https://github.com/username/your-project.git	/* проверка связывания с соответствующим репоз. */

git checkout -- ./ && git pull	/*   Первая команда восстановит всё удалённое/изменённое, вторая вольёт изменения с сервера.	*/

git checkout -- filename	/* восстановление удаленного файла из репозитория */

git branch -v			/*	более детальная информация о коммитах в ветках	*/

#9	3:27